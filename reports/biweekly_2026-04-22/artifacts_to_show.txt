Artifacts to Show at Meeting — April 22, 2026
================================================

1. PROJECT STRUCTURE
   - Show the full repo layout: src/, scripts/, configs/, slurm/
   - Explain the config-driven design (YAML configs → Python scripts)

2. DATA SCHEMA (src/data/schema.py)
   - Walk through the dataclass hierarchy:
     SubsetSample → CandidatePair → Judgment/SentenceJudgment → PreferencePair
   - Highlight JSONL serialization for all stages

3. GCA IMPLEMENTATION (src/judging/gca.py)
   - Show the formula: score = mean(scores) * (min(scores) / mean(scores))^alpha
   - Explain why this penalizes inconsistent sentence quality
   - Show tie_margin logic

4. RELIABILITY CONTROLS (src/judging/reliability.py)
   - Confidence gating at 0.7
   - A/B swap consistency check
   - Evidence rationale validation

5. DEMO: LOCAL SUBSET PREPARATION
   - Run: python scripts/01_prepare_subset.py --config configs/subset.yaml
   - Show output: data/subset/subset_200.jsonl
   - Show run metadata: outputs/runs/<run_id>.json

6. SLURM JOB SCRIPTS
   - Show smoke_test.sh configuration (partition, GPU, time limits)
   - Explain the 3-step smoke test → full generation → judge test workflow

7. IF AVAILABLE: SMOKE TEST RESULTS
   - Output from slurm/smoke_test_<JOBID>.out
   - 5-sample ROUGE scores
   - GPU memory usage

8. BASELINE REFERENCE (from Phase 1-2, 20 samples)
   - ROUGE-1: 0.3278, ROUGE-2: 0.1201, ROUGE-L: 0.2341
   - BERTScore F1: 0.8612
   - These will be the comparison baseline for the 200-sample run
